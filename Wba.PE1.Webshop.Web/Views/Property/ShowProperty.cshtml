@model pe1.Webshop.Web.ViewModels.PropertyDetailVm

@{
    Layout = "_Layout2";
    ViewData["Title"] = "View selections";
    ViewBag.Title = "Our selections";
}

<div class="row justify-content-around align-baseline">

    @{
        foreach (var item in Model.Items)
        {
            @*@Html.Partial("ShowItemsPartial", item)*@
            //<partial name="ShowAllItems" />
            <partial name="ShowItemsPartial" model="item" />
        }

    }
</div>


<p>
    <a href="/categories/all" class="btn btn-primary">&laquo; Back</a>
</p>